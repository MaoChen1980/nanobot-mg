---
name: market-game-analysis
description: "MGA 市场博弈分析（期货+A股）：识别定价决定性力量，趁势而行，在力量放手之前撤退。当用户要求分析市场走势、多空博弈、资金动向、或说「行情怎么看」「主力意图」「XX 品种/股票怎么看」、或提供持仓信息要求分析时激活。当 cron reminder 明确要求「加载 market-game-analysis skill」时激活。禁止跳过 skill_search + read_file SKILL.md 加载链直接执行业务逻辑。"
license: MIT
metadata:
  hermes:
    tags: [finance, futures, trading, analysis, market-game]
    related_skills: [plan, systematic-debugging]
  nanobot:
    original_version: 3.30
    original_repo: github.com/MaoChen1980/nanobot-mg
    adaptation_notes: |
      v4.1 数据链路修复（2026-07-23）：
      - 盘中扫描改用 Node.js 零依赖脚本，不再依赖环境中的 Python 或 AKShare。
      - 连续主力报价只生成候选池；不再声称它提供全市场成交额、实时增仓或席位数据。
      - 增仓、会员持仓、仓单、库存、宏观和新闻的来源及核验边界见 references/data-sources.md。
---

# MGA — Market Game Analysis（新框架 v4.1）

> **核心出发点**：市场价格是由**定价决定性力量**推动的。我们要识别这股力量是谁、想干嘛、什么时候放手，然后趁势而行，不接最后一棒。

## 核心原则

1. **先看数据，后找原因** — 不是先有假设，是先观察到信号，再去调查
2. **因果优先，量价印证** — 因果关系是判断工具，量价关系是印证工具，优先级不能颠倒
3. **识别不了就不做** — 定价决定性力量是入场门槛，不是分析结果
4. **事出反常必有妖** — 量价和因果不匹配时，撤退不需要等确认

---

## When to Use
> 本 skill 覆盖三种主要入口场景，**禁止跳过 skill_search + read_file SKILL.md 加载链直接执行业务逻辑**：

1. **Cron 定时分析**：收到 cron reminder 明确要求「加载 market-game-analysis skill」时 → 必须执行完整加载链（见下方「强制加载链」）
2. **用户发起分析**：用户要求分析市场走势、多空博弈、资金动向、说「行情怎么看」「主力意图」「XX 品种/股票怎么看」、或提供持仓信息时
3. **持仓校验**：用户明确提及持仓、仓位、或询问某个持仓品种时 → 触发 Step 0.5 持仓方向校验

## 强制加载链 ⚠️（收到 cron reminder 时必须执行）

> **触发条件：** cron reminder payload 明确写「请加载 market-game-analysis skill」或类似指令时。
>
> **时序强制：**
> 1. `skill_search("market-game-analysis")` → 返回 SKILL.md 路径
> 2. `read_file(SKILL.md)` 全文 → 必须完整读取后才可执行后续步骤
> 3. 验证触发条件（见下方 5 项）
> 4. 按 Steps 执行
> 5. exec 数据获取
> 6. message 发送

**禁止行为：**
- ❌ 在 read_file SKILL.md 之前执行任何 exec/web_fetch 业务逻辑
- ❌ 用 grep 代替 skill_search 定位 SKILL.md
- ❌ 声称"已加载 skill"但未完成 skill_search + read_file 全链路

### 5 项触发条件验证（exec 前必须全部通过）

| 条件 | 标准 | 未通过处理 |
|------|------|-----------|
| cron reminder 明确要求加载 skill | payload 含「market-game-analysis」或「MGA」 | 不走 skill 加载链 |
| agent 尚未执行 skill_search | 当前轮次无 skill_search 调用 | 必须执行 skill_search |
| agent 尚未 read_file SKILL.md | 当前轮次无 read_file 调用 | 必须 read_file 全文 |
| SKILL.md 路径有效 | `/nanobot/skills/market-game-analysis/SKILL.md` 或 skill_search 返回路径 | 用 skill_search 重新定位 |
| cron 未进入压制期 | assess_me 无「findings + 压制指令」共存 | 按压制协议执行 |

## Step 0 — 市场扫描（每天必做）

### 两种模式

| 模式 | 触发条件 | 扫描范围 |
|------|---------|---------|
| **日常监控** | 无特定触发 | 高流动性连续主力关注列表 |
| **全市场确认** | 需要跨品种成交额、增仓或席位排名 | 交易所或已授权的全市场数据源 |

**全市场扫描触发条件（OR，任一符合即扫描）：**

| 指标 | 标准 |
|------|------|
| 涨跌幅 | > 1%（任一方向，盘中候选） |
| 成交额排名 | 前20名（需要同合约乘数和完整市场数据） |
| 增仓量排名 | 前20名（仅盘后官方持仓差） |
| V/OI 排名 | 前10名（仅盘中热度代理，盘后复核） |

> ⚠️ **做空机会不能漏**：触发条件默认找"异常上涨"，容易漏掉做空机会。每次扫描必须同时检查：
> - **跌幅前排**：跌 > 1%，再核对官方成交额与持仓变化
> - **V/OI < 0.5 缩量下跌**：仅提示可能是多头平仓，不能直接判断资金方向
> - **V/OI > 1 放量下跌**：仅提示需要核对空头席位与官方增仓，不是有效做空的充分条件
> - 如果没有成交额大 + 放量下跌的品种 → 结论里明确写"当前做空机会不成熟"，不得默认全做多

**过滤建议**：盘中至少出现“涨跌异常 + 可解释事件”才进入核验；完整市场数据可得时，再用成交额、增仓和席位做二次过滤。

**扫描结果：**

| 结果 | 后续 |
|------|------|
| 有任一指标触发 | 进入 Step 1 |
| 无任何触发 | 今天结束 |

---

### Step 0 扫描数据来源

> 详见「数据获取方法」章节

先运行 `node references/market-scan.mjs`。它扫描高流动性连续主力合约，输出涨跌、成交/持仓代理与多空候选；它**不是**全市场或席位数据。

再按 `references/data-sources.md` 补齐：
- 用交易所每日行情的同合约持仓差计算增仓；不得从单一实时接口推断增仓。
- 用交易所前二十会员持仓排名判断席位集中度；盘中无可核验数据时，不宣称“主力加仓”。
- 用仓单、库存、供需和事件解释候选品种；关键字段缺失则只列观察，不给开仓结论。

---

## Step 0.5 — 持仓方向校验 ⚠️（每次分析前必做）

> **触发条件：** 用户明确提及持仓、仓位、或询问某个持仓品种时。

### 持仓校验清单

在给出任何操作建议前，必须完成以下校验：

| 校验项 | 操作 |
|--------|------|
| **确认持仓方向** | 用户是多头、空头、还是空仓？方向必须精确到品种 |
| **确认持仓成本** | 用户的开仓价位是多少？当前价位是盈利还是亏损？ |
| **方向一致性检查** | 你的建议方向是否与用户持仓方向一致？ |
| **反向风险提示** | 若建议方向与持仓方向相反，必须明确写出风险 |

### 方向矛盾处理规则

| 场景 | 正确做法 |
|------|---------|
| 用户持有多头，你建议做空 | ❌ 禁止。先说"你的多头持仓建议持有"，再解释为何短线不追多 |
| 用户持有空头，你建议做多 | ❌ 禁止。先说"你的空头持仓已亏损，建议观察/持有"，不追高 |
| 用户空仓，你建议开仓 | ✅ 可以。但必须说清楚是开新仓，不是补现有仓位 |
| 用户持仓方向与你的建议方向一致 | ✅ 可以。给出持有/加仓/止盈建议 |

### 持仓品种逐一覆盖

- **禁止只分析涨跌最大的品种**，忽略用户其他持仓
- 用户持有哪些品种，分析就必须覆盖哪些品种
- 棕榈油在 09:00 有建议但 11:00 未更新 → 必须更新，不能遗漏
- 每个持仓品种都要给出「当前状态 + 建议」

---

## Step 1 — 匹配新闻

- 当前行情有没有对应的外部事件？（地缘/政策/数据发布/产业新闻）
- **有** → 进入 Step 2
- **没有**，或者新闻解释不了行情幅度 → 仍然进入 Step 2（带着"待解释"标记）

---

## Step 2 — 什么资金在作妖？

**核心三问：**

```
1. 什么资金在作妖？
   → 席位数据：哪些席位在加仓？多空席位集中度？
   → 成交量特征：放量/缩量？V/OI 高还是低？
   → 是产业套保？投机资金？国内还是境外？大资金还是散户？

2. 这股资金想干嘛？
   → 建仓？拉升？出货？对敲？
   → 目标价位大概在哪？

3. 这股资金能不能打赢？
   → 这股资金自己的体量够不够？
   → 对手盘是谁？实力如何？
   → 能持续多久？
```

**结果判断：**

| 能识别 | 后续 |
|--------|------|
| 能识别这股资金的类型、意图、体量 | 进入 Step 3 |
| 识别不了 | 结束，不做 |

---

## Step 3 — 知识匹配

- 当前行情是否能用品种知识库里的规律解释？
- **能匹配** → 逻辑成立，进入 Step 4
- **不能匹配** → 可能是新品种类，谨慎处理（减仓或放弃）

---

## Step 4 — 量价印证

**因果逻辑和量价关系是否匹配？**

### 匹配的典型（大资金意图与量价表现对照）

| 因果逻辑 | 匹配的量价表现 |
|---------|--------------|
| 大资金想拉升 | 放量 + 增仓 + 价格上涨 |
| 大资金想出货 | 放量 + 增仓 + 价格不涨或微涨（派发） |
| 大资金想建仓 | 缩量 + 价格区间震荡（吸筹） |
| 空头被迫平仓 | 持仓量降 + 价格涨（空头止损推动） |

### 不匹配的典型

- 成交量翻3倍，价格原地不动
- 新闻利好，但价格暴跌
- 持仓量暴增，价格却横盘
- 缩量上涨，感觉要跌但一直不跌

**结果判断：**

| 匹配状态 | 后续 |
|---------|------|
| 匹配 | 可持仓 / 可开仓 |
| 不匹配 | 触发反向分析 |

### 反向分析（内嵌在 Step 4）

不匹配时触发：
- 这个量价异常背后，有没有没被识别的因果力量？
- **能找到** → 更新因果判断，回到 Step 2 重新验证
- **找不到** → 立即撤退

---

## Step 5 — 退出信号

**因果关系和量价关系开始背离时撤退**

- 不是等止损位到了才跑
- 不是等价格开始跌了才跑
- 是**因果和量价开始对不上**的时候就开始跑

**撤退后：**
- 记录本次行情（时间/品种/因果和量价背离的具体表现）
- 用真实结果验证当初的因果判断
- 更新品种知识库

---

## 品种知识库

> 详见 `references/varieties-knowledge-base.md`

每个品种记录：
- **定价决定性力量在哪一方**
- **什么条件下这方会赢**
- **历史量价表现规律**
- **容易被操纵的特征**（成交额低的品种）

---

## 数据获取

### 盘中候选（连续主力报价）

运行 `node references/market-scan.mjs`，或以 `node references/market-scan.mjs SC0 AU0 AG0` 限定品种；`--json` 输出可保存的结构化结果。

- 该脚本只依赖 Node.js 内置 `fetch`，请求新浪连续主力报价并用报价中的昨结字段计算涨跌。
- 报价中的成交、持仓和 V/OI 仅为热度代理；不得据此计算增仓、成交额排名或会员资金流。
- CFFEX 品种不使用此通道；广期所无夜盘品种须检查报价时间，15:00 定格数据不算夜盘信号。

### 盘后确认与完整信息

按 `references/data-sources.md` 的顺序补齐：

1. 下载交易所每日行情，用同一合约的官方结算价和持仓量确认涨跌及增仓。
2. 查询前二十会员成交/持仓排名，判断多空集中度和方向；没有公开数据时明确写“无法确认席位”。
3. 查询仓单、库存、供需和宏观发布，解释价格异动；新闻只作解释，不能单独作为资金证据。
数据超时、合约映射不清、或第三方价格与官方结算不一致时，结论只能是"观望/待核验"。

### 原油合约标注规范

**原油数据源区分（防止用户混淆）：**

| 原油品种 | 数据来源 | 合约标注 |
|---------|---------|---------|
| 内盘 SC 原油 | akshare futures_fees_info / 新浪主力报价 | 标注「SC 原油主力」或合约代码（如 SC2409） |
| 外盘 WTI 原油 | qhweb.eastmoney.com/quote/yepan（NYMEX） | 标注「NYMEX WTI 原油」 |
| 外盘布伦特原油 | qhweb.eastmoney.com/quote/yepan | 标注「布伦特原油」 |

**铁律：**
- 报告中必须明确标注原油合约类型（内盘 SC vs 外盘 WTI/布伦特）
- 内盘 SC 原油与外盘 WTI/布伦特存在汇率和品质差异，**不可直接对比价格**
- 用户投诉「原油价格不对」时，首先检查是否将内盘 SC 与外盘数据混淆，必要时主动说明合约类型

**用户投诉响应：**
当用户质疑原油数据时：
1. 检查当前使用的原油数据合约类型
2. 若为内盘 SC 原油，主动说明「当前数据为内盘 SC 原油主力，不同于外盘布伦特/WTI，两者价格不可直接对比」
3. 若确实需要对比，提供外盘数据源的价格作为参考（并标注来源）

---

## 框架结构图

```
Step 0: 市场扫描
  → 日常监控（关注列表）
  → 或：全市场扫描（任一指标 OR 触发）

Step 0.5: 持仓方向校验 ⚠️（每次分析前必做）
  → 确认持仓方向 + 成本 + 方向一致性
  → 禁止反向建议（持有空头时建议做多）
  → 持仓品种逐一覆盖

Step 1: 匹配新闻（有没有外部事件）

Step 2: 什么资金在作妖？
  → 席位数据 + 成交量特征
  → 能识别 → 进入 Step 3
  → 识别不了 → 结束，不做

Step 3: 知识匹配（历史规律能否解释）

Step 4: 量价印证
  → 匹配 → 可持仓
  → 不匹配 → 反向分析 → 找到 → 回 Step 2
                      → 找不到 → 立即撤退

Step 5: 退出信号
  → 因果 + 量价背离时撤退
  → 记录，完善知识库
```
- **Self-optimization**: 此 Skill 执行结束时，要检查针对 skill 做出优化。
  - Verification 未通过 → 加载 skill-manager 修复：修正步骤、补充边界情况、修复 bug
  - Verification 全部通过 → 加载 skill-manager 优化：简化步骤、增强执行确定性、改进验证标准
  - **Always 审查**：如果此 skill 的行为已成为 LLM 自然倾向（不注入也会正确执行），将 frontmatter 的 `always: true` 改为 `false`，反之如果发现此 skill 值得每轮注入，改为 `always: true`
