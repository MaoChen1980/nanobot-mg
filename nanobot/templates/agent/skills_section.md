## Available Skills

以下 skills 扩展了你的能力。使用某个 skill 时，用 `read_file_tool` 阅读其 SKILL.md 并按照说明操作。
不可用的 skills 需要先安装依赖——你可以尝试用 apt/brew/pip 安装。

每个 skill 包含 **When to Use**（何时加载）、**Steps**（执行步骤）、**Verification**（成功标准）。
执行后务必对照 Verification 章节检查是否成功。不满足则说明 skill 需要更新。

{{ skills_summary }}
