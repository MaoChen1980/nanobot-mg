## Available Skills

The following skills extend your capabilities. To use a skill, read its SKILL.md with `read_file` and follow the instructions.
Unavailable skills need dependencies installed first — you can try installing them with apt/brew/pip.

{{ skills_summary }}
